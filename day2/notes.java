// OOPs using java
// abstraction, encapsulation, inheritance, polymorphism
// abstraction: hiding internal details and showing only functionality to the user

// encapsulation: wrapping data and code together as a single unit
// inheritance: acquiring properties and methods of an existing class
// polymorphism: the ability of an object to take many forms
// class: blueprint for creating objects
// object: instance of a class
// interface: a reference type in java, it is a collection of abstract methods
// abstract class: a class that cannot be instantiated, it can have abstract and non-abstract methods
// method overloading: same method name with different parameters
// method overriding: providing a specific implementation of a method that is already defined in the parent class
// private constructor: a constructor that is declared as private, it cannot be accessed outside the class, it is used to restrict the instantiation of a class, it is used in singleton design pattern.
// copy constructor: a constructor that creates an object by copying another object, it is used to create a new object with the same state as an existing object.
// static constructor: a constructor that is declared as static, it is used to initialize static variables
// SRP principle: Single Responsibility Principle, a class should have only one reason to change, it should have only one responsibility
// OCP principle: Open/Closed Principle, software entities should be open for extension but closed for modification, it means that you should be able to add new functionality without changing the existing code
// LSP principle: Liskov Substitution Principle, objects of a superclass should be replaceable with objects of its subclasses without affecting the correctness of the program, it means that a subclass should be able to substitute its superclass without affecting the behavior of the program
// ISP principle: Interface Segregation Principle, clients should not be forced to depend on interfaces they do not use, it means that a class should not be forced to implement interfaces it does not use
// DIP principle: Dependency Inversion Principle, high-level modules should not depend on low-level modules, both should depend on abstractions, it means that you should depend on abstractions rather than concrete implementations